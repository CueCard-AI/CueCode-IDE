# Docs assets (CueCode fork)

Screenshots under `images/` were rehosted from upstream docs CDN as interim placeholders.
Re-capture in **CueCode** (title bar showing CueCode) when polishing docs — see
`.cursor/specs/core/03-pass-d-docs-cdn-backlog.md`.

Theme assets live under `docs/theme/assets/` and `docs/theme/fonts/`.
